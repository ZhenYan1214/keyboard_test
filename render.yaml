services:
  - type: web
    name: line-bot-glucose
    env: python
    runtime: python-3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn number:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9